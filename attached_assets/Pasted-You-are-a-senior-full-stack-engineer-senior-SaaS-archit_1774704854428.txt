You are a senior full-stack engineer, senior SaaS architect, senior product designer, senior DBA, senior performance engineer, and senior admin-dashboard specialist.

The Platform Intelligence module has already been rebuilt significantly, but I want you to do the next professional improvement pass.

IMPORTANT GLOBAL REQUIREMENT:
The platform must NOT feel slow.
Everything must remain optimized for smooth performance and scalability for 1000+ concurrent users.
Every change must consider:
- fast rendering
- lightweight frontend updates
- efficient DB queries
- short-lived caching with proper invalidation
- background jobs for heavy work
- no blocking admin actions
- minimal payload size
- concurrency-safe mutations
- scalable image/logo handling

==================================================
CURRENT STATE
==================================================

The module already has:
- improved status model
- async Health Check
- cache layer
- safe delete/disable
- registry table
- platform cards
- detail modal/drilldown
- last checked info
- better API fields

But I still need a final professional pass.

Visible issues / missing improvements:
1. Time display still does not feel fully correct or trustworthy across cards/table/modal
2. There is no proper platform logo / DP upload management option
3. Platform cards, table, and drilldown still need premium UX refinement
4. Some data semantics still need to be clearer
5. I want a more complete operational admin experience

==================================================
MAIN GOAL
==================================================

Take the current Platform Intelligence module and push it to a polished, production-grade final version with:
- correct time semantics
- platform logo upload/management
- clearer operational UX
- stronger admin controls
- better drilldown detail
- cleaner frontend consistency
- preserved speed for 1000+ concurrent users

==================================================
PHASE 1 — DEEP AUDIT OF CURRENT IMPLEMENTATION
==================================================

Inspect the real implementation again before changing anything.

I want you to audit:

1. TIME DISPLAY LOGIC
Check all time-related fields in Platform Intelligence:
- last checked
- last success
- last failure
- last health run
- relative times in cards
- relative times in registry table
- relative times in detail modal
- tooltip/absolute timestamps if any

Verify:
- what timestamp fields are used
- whether they are UTC or local
- whether parsing is safe
- whether relative-time formatting is consistent
- whether “Just now” is overused or misleading
- whether cards/table/modal are using the same timestamp source
- whether “last checked” vs “last success” vs “last activity” are being confused

2. PLATFORM IMAGE / LOGO SYSTEM
Check:
- how platform icons/logos are currently chosen
- whether they are hardcoded
- whether there is a platform image field in DB
- whether upload support already exists somewhere
- whether fallback initials/logo generation exists
- whether logo rendering is safe and cached

3. FRONTEND UX
Check:
- platform cards
- registry table
- detail modal
- action buttons
- filter/search UX
- spacing/hierarchy
- whether anything still feels shallow or incomplete

4. BACKEND / DATA
Check:
- platform schema
- image/logo fields
- file storage approach
- mutation routes
- health timestamps
- drilldown data quality
- any remaining performance bottlenecks for 1000+ concurrent users

Then clearly report:
- what already works
- what still feels weak
- what timing bug/ambiguity remains
- what is needed for logo upload
- what is needed for final polish

==================================================
PHASE 2 — FIX TIME SEMANTICS PROPERLY
==================================================

I want you to fix and standardize Platform Intelligence timing everywhere.

Requirements:
1. Use clear canonical timestamps
2. Distinguish between:
   - last checked
   - last successful check
   - last failed check
   - last activity
3. Ensure cards, table, and drilldown use the correct business timestamp
4. Ensure frontend parsing is timezone-safe
5. Show:
   - relative time in UI
   - absolute timestamp on hover or tooltip
6. Use better display rules:
   - Just now
   - 1m ago
   - 2m ago
   - 1h ago
   - Yesterday
   - 2d ago
   - explicit date for older values
7. Do not require heavy polling or full refreshes

Also inspect whether “Just now” is currently misleading because all last_checked values are being updated at the same time during Health Check.
If so:
- make that clearer in the UI
- or separate “health run time” from “platform-specific last success/failure time”

==================================================
PHASE 3 — ADD PLATFORM LOGO / DP UPLOAD SYSTEM
==================================================

I want a real platform logo management feature.

Implement a safe and professional system for platform image/logo handling.

Requirements:
1. Add platform logo upload option in admin UI
2. Allow:
   - upload new logo
   - replace logo
   - remove/reset logo
3. Support fallback logic:
   - uploaded logo if exists
   - known built-in logo if available
   - initials/avatar fallback if no logo exists
4. Show logo consistently in:
   - platform cards
   - registry table
   - detail modal
5. Validate image uploads:
   - file type
   - file size
   - dimensions if needed
6. Store efficiently:
   - file path or asset URL in DB
   - avoid bloating DB with raw binary unless architecture already supports it
7. Ensure fast rendering and caching
8. Ensure 1000+ concurrent users do not make image delivery slow

Also define:
- whether logos are stored locally or via asset storage
- how cache busting/versioning should work after replacement
- safe default placeholder behavior

==================================================
PHASE 4 — FRONTEND UX FINAL POLISH
==================================================

Improve the Platform Intelligence UI to a premium admin-dashboard level.

A. PLATFORM CARDS
Improve:
- hierarchy
- spacing
- clearer health/status relationship
- better reason text
- better counts layout
- cleaner logo handling
- last checked display
- hover/click affordance
- recommended action if useful

B. REGISTRY TABLE
Improve:
- column alignment
- status clarity
- time clarity
- logo rendering
- sorting/filter usability
- action clarity
- bulk action usability
- better empty/loading states

C. DETAIL MODAL / DRILLDOWN
Improve:
- modal layout
- close button
- spacing
- header with logo + platform meta
- health explanation
- timestamps
- account slot table readability
- reason text
- session/subscriber clarity
- responsive behavior

D. ACTIONS
Improve:
- view details
- disable/enable
- delete
- logo upload/manage
- health check
- refresh

Each action should have:
- loading state
- disabled state
- confirmation if destructive
- success/error feedback
- permission safety

==================================================
PHASE 5 — ADD MISSING HIGH-VALUE FEATURES
==================================================

Identify and implement the most important missing features, such as:

- logo upload/manage action
- last successful health check
- last failed health check
- stale data indicator
- reason tooltip
- status explanation tooltip
- filter by status
- sort by last checked / health / accounts / sessions
- bulk enable/disable if useful
- platform detail reason breakdown
- account linkage drilldown improvements
- audit log entries for platform changes
- recommended action label (e.g. "Unused", "Needs accounts", "Review failures")

Only add features that improve real admin value without making the module heavy or slow.

==================================================
PHASE 6 — BACKEND / PERFORMANCE HARDENING
==================================================

This is mandatory:
keep the module fast for 1000+ concurrent users.

Improve:
1. image/logo delivery strategy
2. query efficiency
3. cache invalidation after logo upload or platform update
4. lightweight detail loading
5. safe file validation and storage
6. timestamp formatting consistency
7. background job safety
8. incremental refresh where possible
9. avoid unnecessary full-module reloads
10. keep platform cards and table rendering efficient

==================================================
PHASE 7 — FILES / DATABASE / API DESIGN
==================================================

If needed, extend the schema and APIs.

Potential things to add:
- platform.logo_url
- platform.logo_asset_key
- platform.updated_at for asset changes
- endpoint for upload/update/remove logo
- endpoint for fetching detail timestamps cleanly
- audit log entries for logo changes

For every backend change define:
- route
- method
- request body
- validation
- response
- permission checks
- cache invalidation behavior

==================================================
PHASE 8 — OUTPUT I WANT
==================================================

After inspecting and improving, give me this exact output:

1. CURRENT ISSUE SUMMARY
- what timing issue remained
- what logo/image functionality was missing
- what UX gaps existed
- what performance risks existed

2. PRODUCT DECISION SUMMARY
- final time semantics
- final logo management model
- final UI behavior
- final performance approach for 1000+ concurrent users

3. FILE-BY-FILE CHANGE PLAN
- exact files changed
- what changed in each

4. IMPLEMENTATION PLAN
- safest order of execution
- how regressions are prevented
- how speed is preserved

5. CODE IMPLEMENTATION
Actually implement:
- timing corrections / standardization
- platform logo upload system
- final GUI polish
- backend and API improvements

6. FINAL QA / RELEASE REPORT
Verify:
- time display is correct and trustworthy
- cards/table/modal use consistent time semantics
- platform logo upload works
- fallback logos work
- UI looks more professional
- performance remains good for 1000+ concurrent users

==================================================
IMPORTANT RULES
==================================================

- Do not give shallow advice
- Inspect the real code first
- Fix semantics before cosmetic polish
- Do not make the platform slow
- Optimize for 1000+ concurrent users
- Preserve existing functionality while improving it
- Keep the UI compact, premium, and admin-friendly
- Think like a lead engineer shipping to production