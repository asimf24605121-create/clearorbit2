Fix full application (frontend + backend + auth + loading system) to production level.

Current issues:
- Entire website stuck on "Loading..."
- Platforms not showing in Create User dropdown
- Overview dashboard shows 0 or loading forever
- Platform Intelligence empty
- APIs work but frontend not rendering data properly
- Likely 401 auth/session issue and broken loading states

Tasks:

1. GLOBAL FETCH SYSTEM
- Create a reusable safeFetch function
- Always include credentials: "include"
- Handle:
  - 401 → redirect to /login.html
  - network errors
  - invalid JSON
- Return consistent structure

2. FIX ALL FRONTEND API CALLS
- Replace all fetch() with safeFetch()
- Remove all silent returns like:
  if (!data.success) return;
- Always handle:
  - success
  - empty data
  - error state
- Ensure no function exits without updating UI

3. LOADING STATE FIX (CRITICAL)
- Ensure every async function:
  - turns loading ON at start
  - turns loading OFF in finally block
- Remove infinite loading states
- Add fallback UI:
  - "No data available"
  - "Failed to load"

4. AUTH + SESSION FIX
- Ensure all API calls include credentials
- Fix session cookie handling:
  - SameSite=Lax
  - HttpOnly enabled
- Handle 401 globally (redirect to login)
- Ensure login persists across pages

5. BACKEND VALIDATION
- Verify all APIs:
  /api/platforms
  /api/users
  /api/sessions
  /api/admin/overview
- Ensure each API:
  - returns valid JSON
  - includes success flag
  - never crashes

6. DATABASE CHECK
- Ensure platforms table has data
- If empty → seed default platforms

7. ERROR LOGGING
- Add console logs for:
  - API responses
  - errors
- Make debugging easier

8. UI IMPROVEMENT
- Replace loaders with:
  - skeleton → then data or message
- No blank screens
- No stuck spinners

9. REPLIT COMPATIBILITY FIX
- Ensure cookies work in iframe preview
- If not, fallback to token-based auth or force redirect

10. FINAL RESULT
- No page should stay stuck on loading
- All dropdowns populated
- Dashboard shows real data
- System behaves like production SaaS app

Do full A to Z debugging.
Fix ALL pages, ALL API calls, ALL loading states.
Do not leave any part broken.