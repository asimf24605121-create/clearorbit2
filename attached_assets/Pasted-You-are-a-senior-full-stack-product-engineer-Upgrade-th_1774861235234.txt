You are a senior full-stack product engineer. Upgrade the **Pricing page** into a **fully polished, production-ready, premium pricing system**.

## Current status

The dropdown/grid issue was fixed because the backend now returns `is_active: true`, and active platforms appear correctly.

Now do a **pro-level upgrade** of the entire pricing module.

---

# 1) Core requirement

Make the Pricing page:

* fully working
* visually premium
* scalable
* easy to manage
* consistent with the rest of the admin dashboard and public buy page

This should feel like a real SaaS pricing system, not a basic form/page.

---

# 2) Functional requirements

## Platform loading

* ensure all active platforms load correctly
* no empty dropdown/grid when API has data
* show graceful empty state if there are truly no active platforms
* show loading skeleton while fetching
* show retry/error state if fetch fails

## Platform selection

* dropdown must populate correctly
* public pricing cards/grid must populate correctly
* selecting a platform must load that platform’s plans immediately
* no stale data after switching platforms

## Plan loading

For each platform, load and display:

* duration
* duration unit (`minutes`, `hours`, `days`)
* account type
* price
* discount if any
* status (active/inactive)
* badge/label if useful

---

# 3) Support full duration pricing

Pricing must support:

* minutes
* hours
* days

This must match the access system already implemented.

### Examples

* 30 minutes
* 2 hours
* 23 hours
* 2 days
* 7 days
* 30 days

Do not assume pricing is day-only.

---

# 4) Admin pricing management

Improve the admin **Pricing** page so admin can:

* select platform
* see all pricing plans for that platform
* add new pricing plans
* edit existing pricing plans
* enable/disable pricing plans
* delete pricing plans if needed
* set account type
* set duration value
* set duration unit
* set price
* set original price / discounted price if supported
* set badge like:

  * Popular
  * Best Value
  * Limited Offer

### Important

The admin page should not be just one empty dropdown.
It must become a complete pricing management UI.

---

# 5) Public pricing page UX

Make the public pricing/buy page look premium.

## Platform cards

Show:

* logo
* platform name
* brand color
* active state
* nice hover effect
* “View Plans” CTA

## Pricing modal / plan section

When clicking a platform:

* open a polished modal or dedicated section
* show all available plans clearly
* group plans in clean cards
* highlight best-value/popular plans
* show duration in human-friendly format
* show exact price clearly
* show purchase CTA cleanly

### Example formatting

* `30 Minutes`
* `2 Hours`
* `23 Hours`
* `2 Days`
* `7 Days`
* `30 Days`

---

# 6) Visual/UX upgrade

Make the pricing system feel premium and modern.

## Design direction

* clean SaaS-style pricing cards
* better spacing
* stronger hierarchy
* subtle shadows
* clean borders
* premium hover states
* responsive layout
* visually balanced cards
* no awkward empty areas
* no plain/basic select-only UI

## Improve:

* dropdown style
* cards
* modal
* plan badges
* CTA buttons
* empty states
* loading states
* error states

---

# 7) Data/API consistency

Audit and fix the whole pricing flow.

## Check and align:

* backend route for public platforms
* backend route for platform pricing
* backend route for admin pricing CRUD
* frontend fetch logic
* mapping fields (`is_active`, `logo_url`, `bg_color_hex`, etc.)
* data shape consistency across all endpoints

### Important

No mismatch between backend and frontend field names.
Standardize the response format.

---

# 8) Validation rules

Add strong validation for pricing plans.

### Validate:

* platform must exist
* duration value must be > 0
* duration unit must be valid
* price must be numeric and > 0
* duplicate conflicting plans should be prevented or warned
* inactive platforms should not show in public pricing

Show clean inline validation messages.

---

# 9) Error handling

Implement professional handling for:

* empty platform list
* no plans for selected platform
* network failure
* malformed API response
* invalid pricing plan creation
* duplicate plan conflicts

Use user-friendly messages, not raw errors.

---

# 10) Responsiveness

Make pricing fully responsive:

## Desktop

* multi-card premium layout

## Tablet

* balanced stacked layout

## Mobile

* clean vertical cards
* easy touch targets
* readable pricing
* modal usable on small screens

---

# 11) Performance

This platform must not be slow and must be optimized for **1000+ concurrent users**.

So:

* avoid unnecessary repeated fetches
* cache platform/pricing data where appropriate
* debounce platform switching if needed
* keep rendering efficient
* no heavy UI lag

---

# 12) Security and safety

Ensure:

* only active platforms show publicly
* inactive/deleted plans do not leak to public UI
* admin-only pricing endpoints remain protected
* public endpoints return only safe fields

---

# 13) Final deliverables

Implement and provide:

1. fully working admin pricing management page
2. fully working public pricing page
3. support for minutes/hours/days plans
4. polished pricing cards and modal
5. loading/empty/error states
6. validated backend + frontend flow
7. explanation of what was broken and what was improved
8. final confirmation that the pricing system is fully production-ready

---

# 14) Final instruction

Do not stop at only fixing `is_active`.

Turn the whole pricing module into a **premium, polished, reliable, production-grade pricing system** that looks and behaves like a real SaaS product.
